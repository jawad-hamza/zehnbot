{
  "name": "chatbot-widget",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "test": "node build.js && node test/smoke.js"
  },
  "dependencies": {
    "esbuild": "^0.21.4"
  },
  "devDependencies": {
    "jsdom": "^26.0.0"
  }
}
